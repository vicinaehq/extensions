### Find
Find Files, _fuzzily_.

## Dependencies:
Fuzzy File depends on [goldfish](https://github.com/sameoldlab/goldfish) to work. It also calls the following depending on OS:
 - file (linux, darwin): for file info
 - xdg-open (linux)
 - explorer (windows)
 - open (darwin)
